<?php
// Marketing / analytics IDs used by the cookie consent banner (includes/cookie-consent.php).
// A tool only loads when its ID is filled in AND the visitor has agreed to that cookie category.
// Leave an ID empty to switch that tool off.

define('GA4_MEASUREMENT_ID', ''); // Google Analytics 4, e.g. 'G-XXXXXXXXXX'   (category: analytics)
define('GOOGLE_ADS_ID', '');      // Google Ads tag, e.g. 'AW-XXXXXXXXXX'        (category: marketing)
define('META_PIXEL_ID', '');      // Meta (Facebook) Pixel, e.g. '123456789012345' (category: marketing)
